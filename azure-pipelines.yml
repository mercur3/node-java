
###############################################################################
# WARNING                                                                     #
# This file is autogenerated. Do NOT edit this file. Run =./build.py= instead #
###############################################################################

trigger:
  - master
pr:
  autoCancel: true
  branches:
    include:
      - master
pool:
  vmImage: ubuntu-latest
jobs:
  - job:
    displayName: Building image on Fedora 41, Java 21
    pool:
      vmImage: ubuntu-latest
    steps:
      - script: |
          echo "Building the images"
          printf "Using %d threads\n" $(nproc)
          echo "------------------------------------------------------\n"
          sed "s/__src_tag_name__/fedora:41/g" -i Dockerfile
          sed "s/__java_version__/21/g" -i Dockerfile
          cat Dockerfile
        displayName: Adding the version

      - script: |
          set -e
          docker build -f Dockerfile -t mercur3/node-java:fedora41-java21 .
          echo "------------------------------------------------------\n"
        displayName: docker build -f Dockerfile -t mercur3/node-java:fedora41-java21 .

      - script: |
          echo "------------------------------------------------------\n"
          echo "$DOCKER_PASSWORD" | docker login -u mercur3 --password-stdin
          docker push mercur3/node-java:fedora41-java21
          rm -rf ~/.docker
        displayName: docker push mercur3/node-java:fedora41-java21
        condition: not(eq(variables['Build.Reason'], 'PullRequest'))
        env:
          DOCKER_PASSWORD: $(DOCKER_PASSWORD)


